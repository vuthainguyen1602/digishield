package com.digishield.learning.domain;

/**
 * Kind of assessment offered by the Learning module.
 */
public enum AssessmentType {
    /** Knowledge check (quiz-style). */
    KNOWLEDGE,
    /** Security-culture survey. */
    CULTURE,
    /** Adaptive placement test used to recommend a starting level. */
    PLACEMENT
}
