import { AppShell, Button, ProgressBar } from '@/shared/ui';

/**
 * CompliancePage — mandatory-policy completion tracking.
 * Pixel-matched to the design handoff "COMPLIANCE" screen.
 *
 * TODO: replace MOCK data with the generated useCompliance() hook from `@/api/generated`.
 */

const kpis = [
  { label: 'Tỉ lệ tuân thủ', value: '91%', sub: '1.128/1.240 người', color: 'var(--color-teal)' },
  { label: 'Đã hoàn thành', value: '5', sub: '/ 6 chính sách', color: 'var(--color-text)' },
  { label: 'Sắp đến hạn', value: '2', sub: 'chính sách', color: 'var(--color-amber)' },
];

type Policy = {
  id: string;
  name: string;
  deadline: string;
  pct: number;
  color: string;
  pillBg: string;
  pillColor: string;
  pillSuffix: string;
};

const policies: Policy[] = [
  {
    id: 'p1',
    name: 'Đào tạo nhận thức an ninh cơ bản',
    deadline: 'Hạn: 31/12/2026 · Bắt buộc mọi nhân viên',
    pct: 94,
    color: 'var(--color-teal)',
    pillBg: 'var(--pill-safe-bg)',
    pillColor: 'var(--pill-safe-fg)',
    pillSuffix: '✓',
  },
  {
    id: 'p2',
    name: 'Chống phishing nâng cao',
    deadline: 'Hạn: 30/06/2026 · Phòng Kế toán, Kinh doanh',
    pct: 71,
    color: 'var(--color-amber)',
    pillBg: 'var(--pill-warning-bg)',
    pillColor: 'var(--pill-warning-fg)',
    pillSuffix: '!',
  },
  {
    id: 'p3',
    name: 'Bảo vệ dữ liệu cá nhân (PDPA)',
    deadline: 'Hạn: 30/09/2026 · Mọi nhân viên',
    pct: 88,
    color: 'var(--color-teal)',
    pillBg: 'var(--pill-safe-bg)',
    pillColor: 'var(--pill-safe-fg)',
    pillSuffix: '✓',
  },
];

const cardStyle: React.CSSProperties = {
  background: 'var(--color-surface)',
  border: '1px solid var(--color-border)',
  borderRadius: 12,
};
const labelStyle: React.CSSProperties = {
  fontSize: 10.5,
  fontWeight: 600,
  color: 'var(--color-muted)',
  letterSpacing: '.08em',
  textTransform: 'uppercase',
};

export default function CompliancePage() {
  return (
    <>
      <div style={{ animation: 'fadeUp .3s ease' }}>
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            marginBottom: 20,
          }}
        >
          <div>
            <div
              style={{
                fontFamily: "'Space Grotesk', system-ui",
                fontSize: 22,
                fontWeight: 700,
                color: 'var(--color-text)',
                letterSpacing: '-.02em',
              }}
            >
              Tuân thủ · Compliance
            </div>
            <div style={{ fontSize: 13, color: 'var(--color-muted)', marginTop: 4 }}>
              Theo dõi việc hoàn thành các chính sách bắt buộc
            </div>
          </div>
          <Button variant="outline">Xuất báo cáo PDF</Button>
        </div>

        {/* KPI tiles */}
        <div
          style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(3, 1fr)',
            gap: 14,
            marginBottom: 20,
          }}
        >
          {kpis.map((k) => (
            <div key={k.label} style={{ ...cardStyle, padding: 20, textAlign: 'center' }}>
              <div style={{ ...labelStyle, marginBottom: 12 }}>{k.label}</div>
              <div
                style={{
                  fontFamily: "'Space Grotesk', system-ui",
                  fontSize: 42,
                  fontWeight: 700,
                  color: k.color,
                  letterSpacing: '-.02em',
                }}
              >
                {k.value}
              </div>
              <div style={{ fontSize: 12, color: 'var(--color-muted)', marginTop: 4 }}>{k.sub}</div>
            </div>
          ))}
        </div>

        {/* Policy list */}
        <div style={{ ...cardStyle, overflow: 'hidden' }}>
          <div style={{ padding: '14px 20px', borderBottom: '1px solid var(--color-border)' }}>
            <div style={{ fontSize: 14, fontWeight: 600, color: 'var(--color-text)' }}>Danh sách chính sách</div>
          </div>
          {policies.map((p, i) => (
            <div
              key={p.id}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 14,
                padding: '14px 20px',
                borderBottom: i < policies.length - 1 ? '1px solid var(--color-border)' : undefined,
              }}
            >
              <div style={{ flex: 1 }}>
                <div style={{ fontSize: 13.5, fontWeight: 500, color: 'var(--color-text)', marginBottom: 3 }}>
                  {p.name}
                </div>
                <div style={{ fontSize: 12, color: 'var(--color-muted)' }}>{p.deadline}</div>
              </div>
              <div style={{ width: 120 }}>
                <ProgressBar value={p.pct} max={100} color={p.color} />
              </div>
              <span
                style={{
                  background: p.pillBg,
                  color: p.pillColor,
                  borderRadius: 99,
                  padding: '3px 10px',
                  fontSize: 11.5,
                  fontWeight: 600,
                  flexShrink: 0,
                }}
              >
                {p.pct}% {p.pillSuffix}
              </span>
            </div>
          ))}
        </div>
      </div>
    </>
  );
}
