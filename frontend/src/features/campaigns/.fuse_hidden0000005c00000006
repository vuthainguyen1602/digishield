import { AppShell, DataTable, StatusPill } from '@/shared/ui';
import type { ColumnDef } from '@/shared/ui';

/**
 * CampaignResultsPage — completed simulation campaign results.
 * Pixel-matched to the design handoff "CAMPAIGN RESULTS" screen.
 *
 * TODO: replace MOCK data with the generated useCampaign(id) hook from
 * `@/api/generated` (GET /sim/campaigns/:id).
 */

const funnel = [
  { label: 'Gửi', value: '1,000', pct: 100, width: 100, color: 'var(--color-blue)', pctColor: 'var(--color-muted)' },
  { label: 'Mở', value: '540', pct: 54, width: 54, color: '#4D86F7', pctColor: 'var(--color-muted)' },
  { label: 'Bấm', value: '132', pct: 13.2, width: 13.2, minWidth: 80, color: 'var(--color-amber)', pctColor: 'var(--color-amber)' },
  { label: 'Nhập liệu', value: '41', pct: 4.1, width: 4.1, minWidth: 60, color: 'var(--color-red)', pctColor: 'var(--color-red)' },
  { label: 'Báo cáo', value: '88', pct: 8.8, width: 8.8, minWidth: 60, color: 'var(--color-teal)', pctColor: 'var(--color-teal)' },
];

type ResultRow = {
  id: string;
  name: string;
  dept: string;
  action: string;
  actionColor: string;
  learning: string;
  learningColor: string;
};

const rows: ResultRow[] = [
  {
    id: '1',
    name: 'Nguyễn Minh An',
    dept: 'Kế toán',
    action: 'Bấm link',
    actionColor: 'var(--color-amber)',
    learning: '⏳ Đang học',
    learningColor: 'var(--color-amber)',
  },
  {
    id: '2',
    name: 'Trần Thị Bình',
    dept: 'Kinh doanh',
    action: 'Báo cáo',
    actionColor: 'var(--color-teal)',
    learning: '—',
    learningColor: 'var(--color-muted)',
  },
  {
    id: '3',
    name: 'Lê Văn Cường',
    dept: 'Kế toán',
    action: 'Bỏ qua',
    actionColor: 'var(--color-muted)',
    learning: '—',
    learningColor: 'var(--color-muted)',
  },
];

const cardStyle: React.CSSProperties = {
  background: 'var(--color-surface)',
  border: '1px solid var(--color-border)',
  borderRadius: 12,
};
const labelStyle: React.CSSProperties = {
  fontSize: 11,
  fontWeight: 600,
  color: 'var(--color-muted)',
  letterSpacing: '.08em',
  textTransform: 'uppercase',
};

export default function CampaignResultsPage() {
  const columns: ColumnDef<ResultRow>[] = [
    {
      id: 'name',
      header: 'Người dùng',
      cell: (r) => <span style={{ fontWeight: 500, color: 'var(--color-text)' }}>{r.name}</span>,
    },
    { id: 'dept', header: 'Phòng ban', cell: (r) => <span style={{ color: 'var(--color-muted)' }}>{r.dept}</span>, width: '120px' },
    {
      id: 'action',
      header: 'Hành động',
      cell: (r) => <span style={{ color: r.actionColor, fontWeight: 500 }}>{r.action}</span>,
      width: '130px',
    },
    {
      id: 'learning',
      header: 'Đã học?',
      cell: (r) => <span style={{ color: r.learningColor }}>{r.learning}</span>,
      width: '140px',
    },
  ];

  return (
    <>
      <div style={{ animation: 'fadeUp .3s ease' }}>
        {/* Header / status */}
        <div
          style={{
            ...cardStyle,
            padding: '20px 24px',
            marginBottom: 14,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
          }}
        >
          <div>
            <div style={{ ...labelStyle, marginBottom: 6 }}>Chiến dịch</div>
            <div
              style={{
                fontFamily: "'Space Grotesk', system-ui",
                fontSize: 20,
                fontWeight: 700,
                color: 'var(--color-text)',
                letterSpacing: '-.01em',
              }}
            >
              "Hoàn tiền học phí"
            </div>
            <div style={{ fontSize: 13, color: 'var(--color-muted)', marginTop: 4 }}>
              Email · Kế toán+Kinh doanh · 05/07/2026
            </div>
          </div>
          <StatusPill variant="safe">Đã hoàn thành</StatusPill>
        </div>

        {/* Funnel */}
        <div style={{ ...cardStyle, padding: 24, marginBottom: 14 }}>
          <div style={{ fontSize: 14, fontWeight: 600, color: 'var(--color-text)', marginBottom: 20 }}>
            Phễu chiến dịch · Campaign Funnel
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
            {funnel.map((f) => (
              <div key={f.label} style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
                <div
                  style={{
                    width: 100,
                    fontSize: 13,
                    color: 'var(--color-muted)',
                    textAlign: 'right',
                    flexShrink: 0,
                  }}
                >
                  {f.label}
                </div>
                <div
                  style={{
                    flex: 1,
                    background: 'var(--color-bg)',
                    borderRadius: 4,
                    height: 28,
                    overflow: 'hidden',
                  }}
                >
                  <div
                    style={{
                      background: f.color,
                      height: '100%',
                      width: `${f.width}%`,
                      minWidth: f.minWidth,
                      display: 'flex',
                      alignItems: 'center',
                      paddingLeft: 12,
                    }}
                  >
                    <span
                      style={{
                        fontSize: 13,
                        fontWeight: 700,
                        color: 'white',
                        fontFamily: "'JetBrains Mono', monospace",
                      }}
                    >
                      {f.value}
                    </span>
                  </div>
                </div>
                <div
                  style={{
                    width: 44,
                    textAlign: 'right',
                    fontSize: 12,
                    color: f.pctColor,
                    fontWeight: f.pctColor === 'var(--color-muted)' ? 400 : 600,
                  }}
                >
                  {f.pct}%
                </div>
              </div>
            ))}
          </div>
          <div
            style={{
              marginTop: 14,
              background: 'rgba(37,102,235,.08)',
              border: '1px solid rgba(37,102,235,.2)',
              borderRadius: 8,
              padding: '12px 16px',
              fontSize: 13,
              color: 'var(--color-blue)',
            }}
          >
            41 người bấm link đã được tự động đăng ký vào "Khóa học nhận diện Phishing"
          </div>
        </div>

        {/* Results table */}
        <div style={{ ...cardStyle, overflow: 'hidden' }}>
          <div
            style={{
              padding: '14px 20px',
              borderBottom: '1px solid var(--color-border)',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
            }}
          >
            <div style={{ fontSize: 14, fontWeight: 600, color: 'var(--color-text)' }}>Danh sách hành động</div>
            <button
              type="button"
              style={{ fontSize: 12, color: 'var(--color-blue)', cursor: 'pointer', background: 'none', border: 'none' }}
            >
              Xuất CSV
            </button>
          </div>
          <DataTable<ResultRow> columns={columns} data={rows} rowKey={(r) => r.id} />
        </div>
      </div>
    </>
  );
}
