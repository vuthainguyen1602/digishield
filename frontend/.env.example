# DigiShield Frontend environment variables
# Copy this file to `.env` (or `.env.local`) and adjust values.

# Base URL of the DigiShield API (see servers[] in docs/DigiShield_openapi.yaml).
# Local dev typically points at a running boot/app instance or a mock server.
VITE_API_BASE_URL=http://localhost:8080/api/v1
